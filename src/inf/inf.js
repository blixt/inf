// Copyright © Andreas Blixt 2010. MIT license.

/**
 * @fileoverview Code generic to the whole inf module.
 */

goog.provide('inf');

/**
 * @define {boolean} Whether debugging should be enabled. If true, additional
 * sanity checks will be performed, and the code will log certain things.
 * Enabling debug reduces performance.
 */
inf.DEBUG = true;
